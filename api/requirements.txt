Flask==1.1.4
Werkzeug==1.0.1

# HTTP stack (one version of requests only)
requests==2.31.0
urllib3==1.26.18
six==1.16.0
charset-normalizer==3.3.2
idna==3.7
certifi==2024.7.4

# Env
python-dotenv==0.14.0

# Firebase / Google (modern + compatible)
firebase-admin==6.4.0
google-auth[requests]==2.35.0
google-cloud-firestore==2.17.0

# Image / color libs used by the project
Pillow==10.2.0
colorgram.py==1.2.0

# (Not used by Vercel functions, but harmless if present)
markupsafe==2.0.1
gunicorn==22.0.0
